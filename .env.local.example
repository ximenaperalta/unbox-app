# Copy this file to .env.local and fill in the values from the Supabase
# project connected in Week 0 (Project Settings > API). These are also
# already set as environment variables in the Vercel project — no new
# secrets are introduced by the /core feature.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
